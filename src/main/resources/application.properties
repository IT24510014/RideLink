spring.application.name=RideLink

# Account Service configuration
server.port=8080

# MongoDB Atlas connection — pulled from the MONGODB_URI environment variable.
spring.data.mongodb.uri=${MONGODB_URI}

# JWT
jwt.secret=${JWT_SECRET}
jwt.expiration-ms=${JWT_EXPIRATION_MS}